var React = require('react');

var Examples = (props) => {
      return (
        <div>
          <h3> Examples component!</h3>
          <p> This is example page !</p>
        </div>
    );
}

module.exports = Examples;
